| Grouping | DULT Spec Section | DULT Requirements | Description | Passing Criteria |
|---|---|---|---|---|
| Advertisement Payload | 3.2.1. Advertising | The accessory SHALL advertise using Bluetooth LE. | Verify that the accessory continuously transmits Bluetooth Low Energy (BLE) advertisement packets. | Bluetooth advertising packets are detected. |
| Advertisement Payload | 3.2.2. Connection | The accessory MUST support at least one non-owner unencrypted connection in a peripheral role. Non-owner connections to the accessory SHALL be implemented using a platform-compatible method, e.g., BT GATT service. | Verify that the accessory accepts at least one unencrypted BLE connection from a non-owner device in a peripheral role, and that the connection is established via a platform-compatible method (e.g., GATT service). | Connection is established from a non-owner device without pairing/bonding. Accessory is confirmed in peripheral role. Non-owner GATT service UUID is present and accessible. Non-owner characteristic UUID is present and responsive. |
| Advertisement Payload | 3.3. Location Tracking | The accessory SHALL maintain an internal state that detects when its location is, or has been, available to the owner via a network. This state is called the location-enabled state. | Verify that the accessory correctly enters and maintains the location-enabled state when its location has been made available to the owner via a network, and exits the state when that condition is no longer met. | Location-enabled advertisement payload is broadcast immediately after a confirmed owner location update. Location-enabled payload continues to be broadcast throughout the 24-hour window. Location-enabled payload ceases after the 24-hour window expires with no new update. |
| Advertisement Payload | Location-enabled Bluetooth LE Advertisement Payload 3.4.1. Overview | When in location-enabled state, the accessory SHALL advertise a Bluetooth LE format, denoted the location-enabled Bluetooth advertisement payload, that is recognizable to the platforms. | Verify that while in the location-enabled state the accessory broadcasts a BLE advertisement payload that conforms to the location-enabled format and is recognized by the owner platform. | Advertisement payload matches the location-enabled format; platform recognizes the accessory. |
| Advertisement Payload | 3.4.1. Location-enabled Bluetooth LE Advertisement Payload Overview / 3.4.2. Location-enabled advertisement payload format | The advertisement MUST allow the owner's platform to reliably recognize the owner's associated accessories, that is a critical signal to distinguish unwanted trackers from expected ones. | Verify that the owner's platform consistently identifies the accessory as an associated device from its advertisement payload, including after MAC address rotation events. | Owner platform reliably recognizes the accessory as an associated device before and after MAC rotation. |
| Advertisement Payload | 3.4.2. Location-enabled Bluetooth LE Advertisement payload format | When Flags TLV are not added, the MAC address type needs to be set to random.  This implies that if Bluetooth LE pairing is supported, the accessory SHALL NOT use its public address as its public identity when exchanging pairing keys at phase 3 and it SHALL only use a static random address. | Verify that when the Flags TLV is absent the accessory uses a random MAC address type in its advertisement, and uses only a static random address (not its public address) during BLE pairing key exchange. | MAC address type is random; static random address used during pairing key exchange. |
| Advertisement Payload | 3.4.3. Duration of advertising location-enabled advertisement payload | The accessory SHALL broadcast the location-enabled advertisement payload if location is available to the owner or was available any time within the past 24 hours. | Verify that the accessory continues broadcasting the location-enabled payload throughout the full 24-hour window after the last owner location update, and stops broadcasting it once the window expires. | Location-enabled payload advertised throughout the 24-hour window; ceases after expiry. |
| Advertisement Payload | 3.4.4. Maximum duration after physical separation from owner to transition into separated mode | The accessory SHALL transition from near-owner mode to separated mode under the conditions listed in Table 2 below. Preferred: The accessory has been physically separated from the owner device for more than 30 minutes. Acceptable: The accessory has been physically separated from the owner device for more than 30 minutes AND The owner of the accessory has received a more recent location update for that accessory after 30 minutes | Verify that the accessory transitions from near-owner to separated mode within the required time window after the owner device leaves BLE range, under both the preferred and acceptable conditions. | Accessory transitions to separated mode within the required time window. |
| Advertisement Payload | 3.4.5. Maximum duration after reunification with owner to transition into near-owner mode | The accessory SHALL transition from separated to near-owner mode if it has reunited with the owner device for a duration no longer than 30 minutes. | Verify that the accessory transitions back to near-owner mode within 30 minutes of the owner device returning to BLE range. | Accessory transitions to near-owner mode within 30 minutes of returning to BLE range. |
| Advertisement Payload | 3.5. MAC address | The Bluetooth LE advertisement payload SHALL contain an address in the 6-byte Bluetooth MAC address field which looks random to all parties while being recognizable by the owner device. | Verify that the 6-byte MAC address in the BLE advertisement appears random to non-owner observers while still being correctly recognized by the owner's platform. | MAC address appears random to non-owners; owner platform correctly identifies the accessory. |
| Advertisement Payload | 3.5. MAC address | The owner MUST be able to predict the MAC address or the payload advertised by the accessory at any given time in order to suppress unwanted tracking alerts caused by a device’s owned accessory. | Verify that the owner's platform can predict the accessory's MAC address or advertisement payload at any point in time, enabling suppression of false unwanted tracking alerts. | Predicted MAC or payload matches the actual advertisement at all tested time points. |
| Advertisement Payload | 3.5. MAC address | The address SHALL rotate periodically; otherwise if the same address is used for long periods of time, an adversary may be able to track a legitimate person carrying the accessory through local Bluetooth LE scanning devices. Same rules apply to all of the advertised payload. | Verify that the MAC address and associated payload values change at regular intervals and are not static, preventing long-term tracking by non-owners. | MAC address and corresponding payload values rotate at the defined intervals. |
| Advertisement Payload | 3.5.1. Rotation policy | An accessory SHALL rotate its address on any transition from near-owner state to separated state as well as any transition from separated state to near-owner state. | Verify that the MAC address rotates immediately upon each state transition — both from near-owner to separated and from separated to near-owner. | MAC address rotates on both near-owner→separated and separated→near-owner transitions. |
| Advertisement Payload | 3.5.1. Rotation policy | When in near-owner state, the accessory SHALL rotate its address every 15 minutes. This is a privacy consideration to deter tracking of the accessory by non-owners when it is in physical proximity to the owner. | Verify that while in near-owner state the accessory rotates its MAC address at approximately 15-minute intervals to deter proximity tracking by non-owners. | MAC address rotates approximately every 15 minutes while in near-owner state. |
| Advertisement Payload | 3.5.1. Rotation policy | When in a separated state, the accessory SHALL rotate its address every 24 hours. This duration allows a platform's unwanted tracking algorithms to detect that the same accessory is in proximity for some period of time, when the owner is not in physical proximity. | Verify that while in separated state the accessory rotates its MAC address every 24 hours, allowing unwanted tracking detection algorithms to function correctly. | MAC address rotates approximately every 24 hours while in separated state. |
| Advertisement Payload | 3.7. Network ID | The 1-byte Network ID SHALL be set based on a registered value for the manufacturer, as defined in Finding Network Registry (Section 10.1). | Verify that the Network ID field in the BLE advertisement payload contains the manufacturer's officially registered 1-byte value from the Finding Network Registry. | Network ID matches the manufacturer's registered value. |
| Advertisement Payload | 3.8. Proprietary company payload | To maintain the privacy properties of the MAC address, the values of payload which may be different between accessories SHALL rotate at the same time and interval as the MAC address. | Verify that all proprietary payload fields that vary between accessories rotate simultaneously with the MAC address and follow the same rotation interval. | Proprietary payload values rotate at the same time and interval as the MAC address. |
| Advertisement Payload | 3.8. Proprietary company payload | If a Resolvable Private MAC address is used, this field SHALL be populated with a value of 6 bytes minimum which allows the platform to recognize an owned accessory without ambiguity to support the identification of owned accessory by the platform as defined in Owned Accessory Identification . | Verify that when a Resolvable Private MAC address is used the associated field is at least 6 bytes in length and enables unambiguous identification of the owned accessory by the platform. | Field is 6 bytes minimum; owner platform identifies the accessory without ambiguity. |
| Advertisement Payload | 3.9. Near-owner bit | In order to allow suppression of unwanted tracking alerts for an accessory advertising the location-enabled advertisement with the owner nearby, the accessory MUST set the near-owner bit to be 1 when in near-owner mode, otherwise the bit is set to 0. The near-owner bit MUST be the least significant bit. | Verify that the near-owner bit in the advertisement payload is correctly set to 1 in near-owner state and 0 in separated state, and that it occupies the least significant bit position. | Near-owner bit is 1 in near-owner state and 0 in separated state; bit is the LSB. |
| Advertisement Payload | 3.10. Bluetooth LE advertising interval | A maximum advertising interval of 4 seconds SHALL be used; for the best detection rate, the advertising interval SHOULD be less than or equal to 2 seconds. | Verify that the time between consecutive BLE advertisement packets does not exceed 4 seconds, and note whether the recommended ≤2 second interval is achieved. | All advertising intervals are ≤ 4 seconds. |
| Opcodes | 3.11. Accessory Connections | The accessory non-owner service UUID SHALL be 15190001-12F4-C226-88ED-2AC5579F2A85. This service SHALL use GATT over LE. The non-owner accessory service SHALL be instantiated as a primary service. The accessory non-owner characteristic UUID SHALL be 8E0C0001-1D68-FB92-BF61-48377421680E. | Verify that the accessory exposes the specified non-owner GATT service as a primary service with the correct characteristic UUID, accessible to non-owner devices in separated state. | Both UUIDs are present; service is a primary service. |
| Opcodes | 3.11.1. Byte transmission order | The characteristic used within this service SHALL be transmitted with the least significant octet first (little endian). | Verify that all multi-byte values transmitted over the non-owner GATT characteristic are encoded in little endian (least significant octet first) byte order. | Response bytes are in little endian order and parsed value matches expected. |
| Opcodes | 3.11.2. Maximum transmission unit | The accessory SHALL NOT request an MTU smaller than the maximum length of its write responses for the opcodes defined in Non-owner controls (Section 3.13.4) and Section 3.12.1. All opcode response data must fit within a single write operation. | Verify that the MTU negotiated by the accessory is large enough to accommodate the longest defined opcode response, and that every response is delivered within a single GATT write operation. | MTU is ≥ the longest response; all responses fit in a single write operation. |
| Opcodes | 3.12. Accessory Information | The following accessory information MUST be persistent through the lifetime of the accessory: Product data, Manufacturer name, Model name, Accessory category, Accessory capabilities, Network ID, and Battery Type. | Verify that all required accessory information fields retain their values after a factory reset or firmware update, confirming they are stored persistently. | All listed fields return the same values after reset/update (excluding network id). |
| Opcodes | 3.12.1. Opcodes | These opcodes SHALL be available when the accessory is in separated state. These opcodes SHALL NOT be available when the accessory is in the near-owner state. When any opcode is not available, the accessory SHALL return the Invalid_command error as the ResponseStatus in Command_Response. If an optional opcode is not available, the accessory SHALL return the Invalid_command error as the ResponseStatus in Command_Response. If any opcode value is commanded that is not supported by the accessory, it SHALL return the Invalid_command error as the ResponseStatus in the Command_Response. | Verify that all defined opcodes respond correctly in separated state and return an Invalid_command error in near-owner state, and that unsupported opcode values also return Invalid_command. | All opcodes return valid responses in separated state; all return Invalid_command in near-owner state; unsupported opcodes return Invalid_command. |
| Opcodes | 3.12.1. Opcodes | In the circumstances that there are multiple non-owner connections, all GATT indication subprocedures defined in Table 4 SHALL be sent through only to the connection that commanded the affiliated write subprocedure. | Verify that when multiple non-owner devices are connected simultaneously, GATT indication responses are directed only to the device that issued the corresponding command, not broadcast to all connections. | GATT indications are sent exclusively to the connection that issued the command. |
| Opcodes | 3.12.1.1. Product data | The Product Data operand represents an 8-byte value that is intended to serve as a unique identifier for the accessory make and model. This value SHALL be determined during the onboarding process (Section 7). | Verify that the Product Data returned by the accessory is exactly 8 bytes in length and matches the unique identifier registered during the manufacturer onboarding process. | Product Data is 8 bytes and matches the registered onboarding value. |
| Opcodes | 3.12.1.2. Manufacturer name | When the Manufacturer Name is less than 64 bytes, it SHALL be formatted either as a string value with length less than 64 bytes or a a string value that is both zero-terminated and zero-padded string up to 64 bytes. | Verify that the Manufacturer Name returned by the accessory is within 64 bytes and correctly formatted as either a plain string or a zero-terminated zero-padded string. | Manufacturer name is correctly formatted and ≤ 64 bytes. |
| Opcodes | 3.12.1.3. Model name | When the Model Name is less than 64 bytes, it SHALL be formatted either a string value with length less than 64 bytes or a a string value that is both zero-terminated and zero-padded string up to 64 bytes. | Verify that the Model Name returned by the accessory is within 64 bytes and correctly formatted as either a plain string or a zero-terminated zero-padded string. | Model name is correctly formatted and ≤ 64 bytes. |
| Opcodes | 3.12.1.8. Firmware version | The firmware revision string SHALL use the x[.y[.z]] format. Major version MUST not be greater than (2^16 - 1). Minor and revision version MUST not be greater than (2^8 - 1). The value MUST change after every firmware update. | Verify that the firmware version string follows the required x[.y[.z]] format, all version components are within their defined numeric limits, and version numbers increment correctly across updates. | Version string is in correct format; values within defined ranges; increments follow spec rules. |
| Opcodes | 3.12.1.11. Network version | The Network Version describes the network specification the accessory complies with. The network revision string SHALL use the x[.y[.z]] format. Major version MUST not be greater than (2^16 - 1). Minor and revision version MUST not be greater than (2^8 - 1). The value MUST change after every network update. | Verify that the network version string follows the required x[.y[.z]] format, all components are within their numeric limits, and the value changes after every network update. | Version string is correct format; values within range; version changes after network update. |
| Non-Owner Finding (Motion detector) | 3.13.2.1. Implementation | After TSEPARATED_UT_TIMEOUT in separated state, the accessory MUST enable the motion detector to detect any motion within TSEPARATED_UT_SAMPLING_RATE1.  If motion is not detected within the TSEPARATED_UT_SAMPLING_RATE1 period, the accessory MUST stay in this state until it exits separated state. | Verify that the accessory enables motion detection after the required timeout in separated state and produces no sound when no motion is detected. | When in a separated state, Motion detection activates after the required timeout; no sound is triggered without motion. |
| Non-Owner Finding (Motion detector) | 3.13.2. Motion detector | If the accessory includes an accelerometer, it MUST be configured to detect an orientation change of ±10° along any two axes of the accessory. | Verify that the accelerometer correctly detects an orientation change of ±10° on any two axes and does not trigger on changes below the 10° threshold. | Motion detected at ±10° on any two axes; no false trigger below 10°. |
| Sound Maker | 3.13.3. Sound maker | The accessory MUST include a sound maker (for example, a speaker) to play sound when in separated state, either periodically or when motion is detected. It MUST also play sound when a non-owner tries to locate the accessory by initiating a play sound command from a non-owner device when the accessory is in range and connectable through Bluetooth LE. The sound maker MUST emit a sound with minimum 60 Phon peak loudness as defined by ISO 532-1:2017. The loudness MUST be measured in free acoustic space substantially free of obstacles that would affect the pressure measurement. The loudness MUST be measured by a calibrated (to the Pascal) free field microphone 25 cm from the accessory suspended in free space. | Verify that the accessory contains a functional sound maker that meets the 60 Phon minimum loudness requirement under ISO 532-1:2017 measurement conditions, and plays sound in all required scenarios. | Peak loudness ≥ 60 Phon measured under spec conditions for all trigger methods. |
| Opcodes | 3.13.4. Non-owner controls | Non-owner controls SHALL use the same service and characteristic UUIDs as defined in Accessory Connections (Section 3.11). | Verify that all non-owner control opcodes (Sound_Start, Sound_Stop, Get_Identifier) are accessible through the same GATT service and characteristic UUIDs defined in Section 3.11. | Non-owner controls use the same service and characteristic UUIDs as defined in Section 3.11. |
| Opcodes | 3.13.4. Non-owner controls | Sound_Start and Sound_Stop SHALL only be available to the platform when the accessory is in the separated state. In all other states, the accessory SHALL return the Invalid_command error as the ResponseStatus in Command_Response. If Identifer Retrieval over Bluetooth LE (Section 3.15.3) is supported, Get_Identifier SHALL only be available when in identifier read state; otherwise, it MUST send Command_Response (Section 3.13.4.1.1) with the Invalid_command as the ResponseStatus. | Verify that Sound_Start and Sound_Stop are only functional in separated state, and that Get_Identifier is only functional during the identifier read state — all other states return Invalid_command. | Commands return success in correct states; Invalid_command returned in all other states. |
| Opcodes | 3.13.4. Non-owner controls | In the circumstances that there are multiple non-owner connections, all GATT indication subprocedures defined in Table 18 SHALL be sent through only to the connection that commanded the affiliated write subprocedure. Sound_Completed MAY be sent over all non-owner connections. | Verify that GATT indication responses for non-owner control commands are routed only to the device that issued the command, while Sound_Completed notifications may be sent to all connected non-owner devices. | Command_Response sent only to commanding device; Sound_Completed may be broadcast to all connections. |
| Sound Maker | 3.13.4.1. Play sound | The Sound_Start opcode is used to play sound on the sound maker. The sound maker MUST play sound for a minimum duration of 5 seconds and a maximum duration of 30 seconds. The RECOMMENDED duration is 12 seconds. | Verify that issuing Sound_Start causes the accessory to play sound for a duration between 5 and 30 seconds, and note whether the 12-second recommended duration is met. | Sound plays between 5 and 30 seconds (inclusive). |
| Opcodes | 3.13.4.1 Play sound | The accessory SHALL confirm the start of the play sound procedure by sending a Command_Response (Section 3.12.4.1.1) with the corresponding CommandOpCode and a ResponseStatus value of Success. The accessory SHALL confirm the completion of the stop sound procedure by sending the Sound_Completed message. | Verify that the accessory sends a Command_Response with a Success status after Sound_Start is issued, and sends a Sound_Completed message after sound has finished or been stopped. | Command_Response with Success received after Sound_Start; Sound_Completed received after Sound_Stop. |
| Opcodes | 3.13.4.1.1. Command Response | The accessory SHALL respond to any invalid opcode with Command_Response and Invalid_command as the ResponseStatus. | Verify that any invalid or unsupported opcode value sent to the accessory results in a Command_Response message with Invalid_command (0xFFFF) as the ResponseStatus. | Command_Response with Invalid_command returned for all invalid/unsupported opcodes. |
| Opcodes | 3.13.4.2. Identifier Payload | The Get_Identifier opcode is used to retrieve identifier lookup payload over Bluetooth LE. The identifier read state MUST be enabled for 5 minutes once the user action is successfully performed. If not in identifier read state, it MUST return Invalid_command (0xFFFF). | Verify that Get_Identifier is gated behind a required physical user action, that the identifier read state window is 5 minutes, and that the accessory returns the correct identifier payload within that window. | Get_Identifier fails before and after 5-minute window (initiated with the physical action); succeeds during active identifier read state. |
| Advertisement Payload | 3.13.4.2. Identifier Payload | It is REQUIRED that the encrypted identifier (which in some cases is the product serial number) be non-identifiable. | Verify that the encrypted identifier payload returned by the accessory cannot be decoded to reveal the serial number or any personally identifiable information without the appropriate decryption key. | Identifier payload is encrypted and non-identifiable without the appropriate key. |
| Disablement | 3.14. Disablement | The accessory SHALL have a way to be disabled such that its future locations cannot be seen by its owner. Disablement SHALL be done via some physical action (e.g., button press, gesture, removal of battery, etc.). | Verify that performing the documented physical disablement action causes the accessory to stop advertising and prevents the owner's platform from receiving any further location updates. | Accessory ceases advertising after physical disablement; owner platform receives no further location updates. |
| Disablement | 3.14.1. Disablement instructions | The accessory manufacturer SHALL provide both a text description and a visual depiction of how to disable the accessory that MUST be available when the platform is online and OPTIONALLY when offline. | Verify that the manufacturer provides both a written description and a visual depiction of the disablement procedure, accessible online through the platform. | Text and visual disablement instructions are available online; offline availability is noted if supported. |
| Disablement | 3.15. Identification | The accessory MUST include a way to uniquely identify it — either via a serial number or other privacy-preserving solution. Guidelines for serial numbers only apply if the accessory supports identification via a serial number. | Verify that the accessory provides at least one form of unique identification — a printed serial number or a privacy-preserving alternative — that is accessible to authorized parties. | A unique identifier (serial number or privacy-preserving equivalent) is accessible. |
| Disablement | 3.15.1. Serial number identification | If a serial number is available, it SHALL be printed and easily accessible on the accessory. The serial number MUST be unique for each product ID. | Verify that the serial number is visibly printed and easily readable on the physical accessory without disassembly, and that it is unique across all units sharing the same product ID. | Serial number is printed, accessible, and unique per product ID. |
| Identifier retrieval | 3.15.2. Identifier retrieval capability | The identifier payload SHALL be readable either through NFC tap (Section 3.15.5) or Bluetooth LE (Section 3.15.3). | Verify that the accessory's identifier payload can be successfully retrieved through at least one of the two supported methods: NFC tap or Bluetooth LE. | Identifier payload is successfully retrieved via NFC or BLE (at least one method works). |
| Identifier retrieval | 3.15.3. Identifier retrieval over Bluetooth LE | Accessories that support identifier retrieval over Bluetooth LE MUST have a physical mechanism that SHALL be required to enable the Get_Identifier opcode. | Verify that without performing the required physical action the Get_Identifier opcode is blocked, and that after performing the physical action the identifier payload is returned successfully. | Get_Identifier is gated behind a required physical mechanism; fails without it and succeeds after. |
| Identifier retrieval | 3.15.3. Identifier retrieval over Bluetooth LE | The accessory manufacturer SHALL provide both a text description and a visual depiction of how to enable identifier retrieval over Bluetooth LE, accessible online. | Verify that the manufacturer provides both written and visual instructions explaining how to perform the physical action needed to enable BLE identifier retrieval, accessible online through the platform. | Text and visual instructions for enabling BLE identifier retrieval are available online. |
| Identifier retrieval | 3.15.4. Identifier retrieval from a server | The identifier payload returned from an accessory in the paired state SHALL be encrypted. | Verify that when the accessory is in a paired/associated state the identifier payload returned via BLE or server retrieval is encrypted and cannot be decoded without the appropriate decryption key. | Identifier payload is encrypted in associated state and cannot be decoded without the key. |
| Identifier retrieval | 3.15.5. Identifier over NFC | An associated accessory SHALL advertise the whole URL with arguments as the payload over NFC in the format: `https://{URL}?pid=%04x&b=%02x&fv=%08x&e=%s` | Verify that tapping an NFC-enabled device on the accessory returns an NDEF URI payload formatted as the specified URL with all required arguments correctly populated. | NFC payload is a valid URL in the specified format with all required arguments. |
| Identifier retrieval | 3.15.5. Identifier over NFC | The URL SHALL be hosted by the network provider. The URL SHALL decrypt the identifier payload and return the identifier in a form renderable in HTML. The encrypted identifier SHALL be passed as argument "e" when in associated state. | Verify that the NFC URL is hosted by the network provider, that the server decrypts the identifier and returns it in a browser-renderable HTML format, and that the encrypted identifier is passed as the 'e' URL argument. | URL is hosted by network provider; server decrypts and renders identifier in HTML; 'e' argument contains encrypted identifier. |
| Identifier retrieval | 3.16 Owner registry | Verifiable identity information of the owner of an accessory at time of association SHALL be recorded and associated with the identifier of the accessory, e.g., phone number, email address. | Verify that when an accessory is associated with an owner the network provider's registry records the owner's verifiable identity and links it to the accessory identifier at the time of association. | |
| Identifier retrieval | 3.16.1. Obfuscated owner information | A limited amount of obfuscated owner information from the owner registry SHALL be made available to the platform along with a retrieved identifier. This MUST include at least one of: last four digits of phone number or obfuscated email address. | Verify that the identifier retrieval server response includes at least one form of appropriately obfuscated owner contact information, without exposing any unobfuscated personal data. | At least one obfuscated owner info format is present; no unobfuscated personal data is exposed. |
| Identifier retrieval | 3.17. NFC Requirements | The accessory MUST use a programmable NFC tag with NDEF URI format, type 2 or greater. The NFC tag SHALL not be scannable when in packaging. The payload MUST be scannable at the center of the NFC tag. The NFC implementation MUST be configured as a NFC tag. | Verify that the NFC tag on the accessory meets all hardware requirements: NDEF URI format, type 2 or greater, not scannable in packaging, scannable when unpackaged at the tag center, and configured in passive tag mode. | NFC not scannable in packaging; scannable at center of tag; type ≥ 2; configured as tag; NDEF URI format correct. |
| Opcodes | 4. Accessory Category Value | Accessory manufacturer's MUST pick an accessory category value that closest resembles their physical product. If none of the categories match, Other MUST be chosen. | Verify that the accessory reports a category value that correctly represents its physical product type as defined in Table 21, or uses the 'Other' category when no match exists. | Accessory category matches the closest physical product type; 'Other' used when no category matches. |
| General | 6.1.3. Network Access | Network access MUST NOT be required in the moment that the platform performs owned accessory recognition. | Verify that the owner's platform successfully recognizes the accessory as an owned device without any active network connectivity at the moment of recognition. | Owner device recognizes the accessory as owned without network connectivity. |
| General | 6.1.2. Platform Software Extension | Accessory manufacturers SHALL provide recognizable values to the platform, along with an offset and length indicating what part of the advertisement to match. This set MUST be large enough to accommodate a time offset between the accessory and owner's host platform. | Verify that the manufacturer-provided recognizable value set, with its defined offset and length, correctly identifies the accessory across multiple advertisement payloads and tolerates expected time offsets between the accessory and host platform. | |
| General | 6.1.4. Removal | The platform MUST delete any local identifying information associated with an accessory if the manufacturer's software is removed or if the platform unassociates from the accessory. | Verify that all locally stored identifying information for the accessory is permanently deleted from the platform when the user unassociates the accessory or removes the manufacturer's software. | |
